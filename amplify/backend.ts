import { defineBackend } from '@aws-amplify/backend';
import { defineFunction } from '@aws-amplify/backend';

// Lambda関数の定義（プレースホルダー）
// 実際の実装は後続のタスクで行います

const backend = defineBackend({
  // TODO: Lambda関数とDynamoDBテーブルの定義を追加
});

export default backend;
